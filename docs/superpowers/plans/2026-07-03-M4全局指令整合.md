# M4 全局指令整合 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 双工具全局指令收敛单一源——共享规则进 `vault/memory/00-RULES/operating-rules.md`(hook 常驻注入),native 文件瘦成入仓 stub 软链派生。

**Architecture:** 内容三分(共享/Claude 专属/Codex 专属);注入走既有机制 A(`inject_index` 默认 files 元组扩一行,法律在前导航在后);native 派生走 commands 同款软链模式;软链切换是仓外副作用,由主会话在合并 main 后执行,带备份与回滚。

**Tech Stack:** Markdown + Python stdlib(hook)+ unittest/pytest + symlink。

**Spec:** `docs/superpowers/specs/2026-07-03-M4全局指令整合-design.md`(D1–D8)

## Global Constraints

- 仓库 `~/Library/CodeRepo/AI/AiPalace`;Task 1 起手建特性分支 `git switch -c feature/m4-global-instructions`;Task 1–4 在分支上由 subagent 执行,**Task 5(软链切换)必须在合并 main 之后由主会话执行**(分支未合时软链指向的仓内文件在 main 不存在 → 悬空)。
- commit 格式(hook 强制):`<type>(<scope>): <中文主题>`;正文尾行 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 禁止 `git add -A`/`git add .`;只暂存任务列名文件;绝不暂存 `.superpowers/` 与 `vault/.obsidian/`。
- 文件内容逐字使用本计划给出的全文;vault 文件 frontmatter 硬标准 9 字段。
- `~/.claude/` 与 `~/.codex/` 是挂载点:Task 1–4 期间**绝不动**其中任何文件(备份/软链全在 Task 5)。
- 注入顺序铁律(spec D4):`("00-RULES/operating-rules.md", "INDEX.md")`。

---

### Task 1: 原件快照存档 + operating-rules.md 共享规则 + 白名单

**Files:**
- Create: `docs/archive/native-globals-2026-07-03/claude-CLAUDE.md`(原件快照)
- Create: `docs/archive/native-globals-2026-07-03/codex-AGENTS.md`(原件快照)
- Create: `vault/memory/00-RULES/operating-rules.md`
- Modify: `tools/memory/config.toml`(routing 白名单加一行)

**Interfaces:**
- Produces: `vault/memory/00-RULES/operating-rules.md`(Task 3 注入、Task 2 stub 指针、Task 5 验证共同依赖);首个 H1 含字面标识 `[操作规则]`(stub 兜底指针的判断锚点)。

- [ ] **Step 1: 建特性分支**

```bash
cd ~/Library/CodeRepo/AI/AiPalace && git switch -c feature/m4-global-instructions
```

Expected: `Switched to a new branch 'feature/m4-global-instructions'`

- [ ] **Step 2: 原件快照入仓(P8 留痕,逐字节拷贝)**

```bash
mkdir -p docs/archive/native-globals-2026-07-03
cp ~/.claude/CLAUDE.md docs/archive/native-globals-2026-07-03/claude-CLAUDE.md
cp ~/.codex/AGENTS.md docs/archive/native-globals-2026-07-03/codex-AGENTS.md
diff ~/.claude/CLAUDE.md docs/archive/native-globals-2026-07-03/claude-CLAUDE.md && echo OK1
diff ~/.codex/AGENTS.md docs/archive/native-globals-2026-07-03/codex-AGENTS.md && echo OK2
```

Expected: OK1、OK2(快照与原件逐字节一致;此步只读原件,不改动)

- [ ] **Step 3: 写 operating-rules.md(全文)**

写入 `vault/memory/00-RULES/operating-rules.md`:

```markdown
---
title: 操作规则 · 全局共享(always-on)
type: principle
scope: global
status: active
confidence: high
created: 2026-07-03
updated: 2026-07-03
last_confirmed: 2026-07-03
source: [M4 整合自 ~/.claude/CLAUDE.md 与 ~/.codex/AGENTS.md, ADR-0016]
---

# [操作规则] 全局共享规则(SessionStart 注入)

> 双工具(Claude Code / Codex)工具无关共享规则的唯一源(M4/ADR-0016),经 SessionStart hook 每会话注入。
> 工具专属规则见 native stub(`context/native/`);本文件是 00-RULES 第二个获准 always-on 项(ADR-0014 豁免边界见 ADR-0016)。

## 个人偏好

- **结构化思考**:复杂问题先系统拆解成逻辑组件,再给方案。
- **客观同侪**:事实准确优先于讨好;有理有据地直接指出错误假设与误信息,给可执行的改进。
- **默认语言**:始终用简体中文回复,除非用户明确要求其他语言。

## 查文档 — Context7 MCP 优先

需要库/框架/SDK/API/CLI 的当前文档(语法、签名、配置、版本差异、迁移、"用 Y 库怎么做 X")时,先用 context7 MCP,早于 WebSearch/WebFetch/凭记忆——API 表面会漂移,不靠记忆。凡"当前文档可能改变答案"的场合就查;对外部包写非平凡代码前先查。
调用细则 → `~/Library/CodeRepo/AI/AiPalace/context/howto/context7-mcp.md`(要发起调用时再读取)。

## 维护指令文件(CLAUDE.md / AGENTS.md)

维护任意位置的 `CLAUDE.md` / `AGENTS.md` 前,先读约定 → `~/Library/CodeRepo/AI/AiPalace/context/howto/instruction-file-maintenance.md`。

## 个人配置目录 — `~/Library/ConfigFile/`

该目录(含所有子目录)混放敏感文件(API-key / 凭证 / token / 密码)与普通配置(如 Maven `settings.xml`)。策略「默认审慎、按需授权」:

- **写入 / 拷贝进该目录**、**`ls` / `find` 列目录浏览结构**:自由,无需额外授权。
- **读取任何文件内容**(含 `Read` 及 `cat` / `head` / `tail` / `grep` / `less` / `more` / `od` / `xxd` / `strings` 等回显命令):默认不读——先 `ls` 浏览 → 就具体文件向用户说明"读哪个、为什么"并取得授权后再读。
- **敏感文件(api-key / 凭证类)**:即便被要求也先提醒风险;普通配置经授权可正常读取。

本规则是第二道防线:各工具另有工具层拦截(第一道防线,实现见各自 native stub),绕过工具层也须遵守。

## 查生产/测试数据库 — dbq 只读通道

需要查库数据辅助排查(结合代码定位 bug / 核对线上数据)时,用只读入口 `/Users/dalwin/Library/ConfigFile/db/dbq <实例> "<SQL>"`(实例见 `dbq --list`)。

- **只读铁律**:通道仅 SELECT;要改数据由 AI 查询 + 读码分析后**产出 SQL 交人工执行**,不绕过通道写库(测试环境亦然)。
- **禁读** `ConfigFile/db/` 下连接配置(deny + hook 强制);要查库直接用 dbq,不必拿连接信息。

细则 → `~/Library/CodeRepo/AI/AiPalace/context/howto/db-readonly-cli.md`(要查库时再读取)。

## superpowers 技能 — ask-first

任务命中下列场景,**先简述拟用哪个 superpowers 技能及理由、征得用户同意后再 invoke**(ask-first:不静默自动调用,也不无视):

- 新功能 / 新组件 / 改行为 / 设计 → `brainstorming`(动手前)
- bug / 测试失败 / 非预期行为 → `systematic-debugging`(提修复方案前)
- 实现功能或修复 → `test-driven-development`(写实现前)
- 多步任务、有 spec / 计划 → `writing-plans` → `executing-plans`
- 声称完成 / 修复 / 通过,提交或建 PR 前 → `verification-before-completion`

用户明确说"直接做 / 别走流程 / 别用 skill"时**跳过**。软约束,不覆盖用户显式指示(优先级:用户显式指示 > 本软约束 > 默认行为)。各工具是否有自动注入见各自 native stub 前言。

## 跨工具 skill 单一源

AiPalace(`~/Library/CodeRepo/AI/AiPalace`)是共享 skills 与个人上下文的唯一 SOT:skill 在 `registry.yaml` 声明,由 `tools/skillctl.py` 派生软链到 `~/.claude/skills` 与 `~/.codex/skills`——不得手改派生软链或拷贝 skill 目录;改 registry 后重新 sync。

## 维护 AiPalace 仓库时

遵循其设计哲学与治理:`PHILOSOPHY.md`(P1–P9,最高准绳)+ `docs/governance/`(资产维护规范)。只改声明源(registry/INDEX),工具派生;不手碰派生挂载;动核心配置先勘察再出方案;决策以 append-only ADR 留痕。
```

- [ ] **Step 4: config.toml routing 白名单加 operating-rules**

修改 `tools/memory/config.toml` 的 `[routing] rules_files`:

```toml
rules_files = [
  "00-RULES/identity.md", "00-RULES/tech-stack.md",
  "00-RULES/dev.md", "00-RULES/flow.md", "00-RULES/ops.md",
  "00-RULES/operating-rules.md",
]
```

- [ ] **Step 5: 验证与提交**

```bash
python3 -m unittest discover -s tools/memory 2>&1 | tail -2   # 26 例应仍全绿(白名单是数据)
git add docs/archive/native-globals-2026-07-03/
git commit -m "docs(archive): M4 前 native 全局指令原件快照存档"
git add vault/memory/00-RULES/operating-rules.md tools/memory/config.toml
git commit -m "feat(vault): M4 共享操作规则单一源 operating-rules.md+晋升白名单登记"
```

---

### Task 2: context/native/ 双 stub(入仓真源)

**Files:**
- Create: `context/native/claude-global.md`
- Create: `context/native/codex-global.md`

**Interfaces:**
- Consumes: Task 1 的 `[操作规则]` 锚点语义(兜底指针文案引用)。
- Produces: Task 5 软链目标(`~/.claude/CLAUDE.md → claude-global.md`、`~/.codex/AGENTS.md → codex-global.md`)。

- [ ] **Step 1: 写 claude-global.md(全文)**

```markdown
# Claude Code 全局指令(受管 stub)

> **受管派生**:本文件真源在 `AiPalace/context/native/claude-global.md`,`~/.claude/CLAUDE.md` 为其软链(P1/P2,ADR-0016)——改真源即生效,勿手改派生。
> **共享规则**:工具无关全局规则(个人偏好 / Context7 / ConfigFile 审慎 / dbq / ask-first / 跨工具 SOT / AiPalace 治理)单一源在 `vault/memory/00-RULES/operating-rules.md`,经 SessionStart hook 每会话注入。**若本会话开头未见 `[操作规则]` 注入块,先 Read `~/Library/CodeRepo/AI/AiPalace/vault/memory/00-RULES/operating-rules.md`。**

## Claude 专属

- **superpowers 注入**:Claude Code 经插件 SessionStart 自动注入 `using-superpowers`;共享规则中的 ask-first 清单在本侧为双保险兜底。
- **ConfigFile 第一道防线**:`~/.claude/settings.json` 的 `permissions.deny` 已对 `ConfigFile/**` 读取做工具层拦截;共享规则中的审慎策略为第二道防线。
```

- [ ] **Step 2: 写 codex-global.md(全文)**

```markdown
# Codex 全局指令(受管 stub)

> **受管派生**:本文件真源在 `AiPalace/context/native/codex-global.md`,`~/.codex/AGENTS.md` 为其软链(P1/P2,ADR-0016)——改真源即生效,勿手改派生。
> **共享规则**:工具无关全局规则(个人偏好 / Context7 / ConfigFile 审慎 / dbq / ask-first / 跨工具 SOT / AiPalace 治理)单一源在 `vault/memory/00-RULES/operating-rules.md`,经 SessionStart hook 每会话注入。**若本会话开头未见 `[操作规则]` 注入块,先 Read `~/Library/CodeRepo/AI/AiPalace/vault/memory/00-RULES/operating-rules.md`。**

## Codex 专属

- **Workflow Memory Boundary**: Treat AGENTS.md as the source for stable, always-on behavior. Use memories and hooks only as auxiliary recall or context hints. Do not rely on memories for hard requirements that must always apply.
- **ConfigFile 第一道防线**:`~/.codex/hooks/pretooluse-configfile-guard.py` 对该目录读取返回 permissionDecision="ask";共享规则中的审慎策略为第二道防线。
- **superpowers 注入**:Codex 无 SessionStart 自动注入 `using-superpowers`,共享规则中的 ask-first 清单即本侧兜底触发器。
```

- [ ] **Step 3: 提交**

```bash
git add context/native/claude-global.md context/native/codex-global.md
git commit -m "feat(context): M4 native 全局指令 stub 入仓(claude/codex 双真源)"
```

---

### Task 3: hook 双文件注入(TDD)

**Files:**
- Modify: `tools/hooks/inject_index.py`(默认 files 元组一行 + docstring)
- Test: `tools/hooks/test_sessionstart.py`、`tests/test_inject_index.py`(各加 1 用例)

**Interfaces:**
- Consumes: `inject_index(context_root, files=(...))` 既有签名;`sessionstart.build_output(cwd, ctx)`(190 行调用 `inject_index(context_root)` 走默认参,**不改**)。
- Produces: 默认注入 = `("00-RULES/operating-rules.md", "INDEX.md")`,两入口(sessionstart.build_output 与 inject_index.main)同时生效;缺文件仍容错跳过。

- [ ] **Step 1: 写失败测试**

`tools/hooks/test_sessionstart.py` 的 `BuildOutput` 类追加:

```python
    def test_operating_rules_injected_before_index(self):
        with tempfile.TemporaryDirectory() as d:
            ctx = self._ctx(d)
            rules_dir = Path(ctx) / "00-RULES"
            rules_dir.mkdir()
            (rules_dir / "operating-rules.md").write_text("[操作规则]-MARKER", encoding="utf-8")
            out = sessionstart.build_output(Path("/some/plain"), ctx)
            self.assertIn("[操作规则]-MARKER", out)
            self.assertLess(out.index("[操作规则]-MARKER"), out.index("VAULT-INDEX-MARKER"))
```

`tests/test_inject_index.py` 追加(沿用该文件既有 import 与 fixture 风格):

```python
def test_operating_rules_injected_before_index(tmp_path):
    (tmp_path / "00-RULES").mkdir()
    (tmp_path / "00-RULES" / "operating-rules.md").write_text("[操作规则]-MARKER\n", encoding="utf-8")
    (tmp_path / "INDEX.md").write_text("# vault INDEX\n", encoding="utf-8")
    out = inject_index(str(tmp_path))
    assert "[操作规则]-MARKER" in out
    assert out.index("[操作规则]-MARKER") < out.index("vault INDEX")
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python3 -m unittest discover -s tools/hooks -v 2>&1 | tail -5 && python3 -m pytest tests/ -q 2>&1 | tail -2`
Expected: 两处新用例 FAIL(顺序断言失败——operating-rules 尚未进默认注入,`out` 中无 MARKER 触发 ValueError/断言失败);旧用例全绿。

- [ ] **Step 3: 改 inject_index.py 默认元组(最小实现)**

`tools/hooks/inject_index.py` 第 7 行:

```python
def inject_index(context_root, files=("00-RULES/operating-rules.md", "INDEX.md")):
```

模块 docstring(1–4 行)更新为:

```python
"""SessionStart hook:把 vault/memory 的 00-RULES/operating-rules.md(共享操作规则,法律在前)
与 INDEX.md(决策树导航,在后)拼成 always-on 注入文本(ADR-0016)。
双工具通用:Claude 经 additionalContext、Codex 经 SessionStart hook 输出同一文本。
仓库内准备;实际注册到 ~/.claude/hooks、~/.codex/hooks 属各工具接线。"""
```

(`main()` 里 `inject_index(root)` 走新默认参,无需改;`sessionstart.py:190` 同理不改。)

- [ ] **Step 4: 跑测试确认通过**

Run: `python3 -m unittest discover -s tools/hooks -v 2>&1 | tail -3 && python3 -m pytest tests/ -q 2>&1 | tail -1 && python3 -m unittest discover -s tools/memory 2>&1 | tail -2`
Expected: hooks 16 例 OK / pytest 33 passed / memory 26 例 OK(全量无回归)

- [ ] **Step 5: 真仓冒烟(此时分支上 operating-rules.md 已存在)**

Run: `python3 tools/hooks/inject_index.py | python3 -c "import json,sys; t=json.load(sys.stdin)['hookSpecificOutput']['additionalContext']; print('[操作规则]' in t, t.index('[操作规则]') < t.index('INDEX'))"`
Expected: `True True`

- [ ] **Step 6: 拆两 commit 提交**

```bash
git add tools/hooks/inject_index.py
git commit -m "feat(hooks): M4 注入扩为 operating-rules+INDEX 双文件(法律在前)"
git add tools/hooks/test_sessionstart.py tests/test_inject_index.py
git commit -m "test(hooks): M4 双文件注入顺序用例(两测试套)"
```

---

### Task 4: 文档收口 + ADR-0016

**Files:**
- Modify: `docs/governance/content-assets/vault.md`(§2 00-RULES 段、§8 过渡态)
- Modify: `docs/governance/evolution.md`(§6.2 销项)
- Modify: `docs/governance/product-assets/injection.md`(机制 A + native stub 派生)
- Modify: `vault/memory/INDEX.md`(常驻语 + 树索引)
- Create: `adr/0016-M4全局指令整合-共享规则单一源与native-stub派生.md`

**Interfaces:**
- Consumes: Task 1–3 交付事实(文件路径/注入顺序/白名单)。

- [ ] **Step 1: vault.md 两处**

§2「00-RULES(身份层)」的 always-on 要点(现文以「**always-on 模型(ADR-0014)**:唯一常驻 = `identity.md`」开头)改为:

```markdown
- **always-on 模型(ADR-0014 + ADR-0016)**:常驻两项——`identity.md`(精简身份卡,经 INDEX 指引读取)与 `operating-rules.md`(双工具共享操作规则,SessionStart hook 直注,M4 唯一豁免);技术栈与操作规则组(`dev.md` / `flow.md` / `ops.md`)由 `INDEX.md` 决策树**按需拉、可叠加**(多组命中并集加载,如"多步 Java 修复"→ dev + flow)。
```

§2 的「等价于原 `context/self/`(已迁移),并吸收来自全局指令文件的工具无关共享规则(M4 完成后)。」改为:

```markdown
- 等价于原 `context/self/`(已迁移);全局指令文件的工具无关共享规则已整合入 `operating-rules.md`(M4 完成,ADR-0016),native 侧(`~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`)为 `context/native/` 双 stub 的软链派生。
```

§8「过渡态声明(P9)」整节(含 `> ⚠️ **M4 全局指令整合未完成**…` 引块)替换为:

```markdown
## 8. 过渡态声明(P9)

> M4 全局指令整合已完成(ADR-0016):共享规则单一源 `00-RULES/operating-rules.md` + native stub 软链派生,原双存已收敛。native memory(`~/.claude/projects/…/memory/`)存量条目仍按 [`memory.md`](memory.md) 既有策略渐进迁移,属正常运行态,非待决项。
```

- [ ] **Step 2: evolution.md §6.2 销项**

「### 6.2 M4 全局指令整合 + native 瘦身 —— 待做」整节(标题 + 现状 + 收敛路径 4 步)替换为:

```markdown
### 6.2 ~~M4 全局指令整合 + native 瘦身~~ —— 已收敛(2026-07-03,ADR-0016)

**结果**:共享规则单一源 `vault/memory/00-RULES/operating-rules.md`(SessionStart hook 双工具注入,顺序:操作规则→INDEX);native 全局文件瘦为 `context/native/{claude-global,codex-global}.md` 双 stub 并经软链派生(`~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`),原件快照存档 `docs/archive/native-globals-2026-07-03/`。设计:spec `2026-07-03-M4全局指令整合-design.md`(D1–D8)。
```

- [ ] **Step 3: injection.md 机制 A 更新**

「注入机制总览」表机制 A 行的注入内容列 `vault/memory/INDEX.md` 改为 `00-RULES/operating-rules.md + INDEX.md`。

「机制 A」目的段(「在会话启动时把 `vault/memory/INDEX.md` 整体注入…」)改为:

```markdown
在会话启动时按序注入 `vault/memory/00-RULES/operating-rules.md`(双工具共享操作规则,法律在前)与 `vault/memory/INDEX.md`(决策树导航,在后),保证会话始终持有全局约定与记忆索引,无需人工粘贴(ADR-0016)。
```

「机制 A」末尾追加小节:

```markdown
### native 全局文件派生(M4,ADR-0016)

`~/.claude/CLAUDE.md` 与 `~/.codex/AGENTS.md` 为 `context/native/{claude-global,codex-global}.md` 的**软链派生**(同 commands 模式):stub 只留工具专属规则 + 受管头注 + 兜底指针(hook 失效时手读 operating-rules 绝对路径);工具无关共享内容一律在 `operating-rules.md`,不回流 native。改 stub 走仓内真源,勿手改派生。
```

文档顶部 ⚠️ 过渡态说明(ADR-0013 引块)末尾追加一句:`M4 已完成(ADR-0016):机制 A 现为双文件注入,native 全局文件为受管 stub 派生。`

- [ ] **Step 4: INDEX.md 两处**

顶部常驻语(`> 常驻:进会话只读 \`00-RULES/identity.md\`(精简身份卡);…`)改为:

```markdown
> 常驻两项:`00-RULES/operating-rules.md`(共享操作规则,已随本 INDEX 由 hook 直注,**无需再读**)+ `00-RULES/identity.md`(精简身份卡,进会话必读);其余(tech-stack / 操作规则组 / 各域知识)按下方决策树命中再拉。契约见 [[PROTOCOL]]。
```

「当前整树文件索引」的 00-RULES 小树在 `identity.md` 行后加:

```
│   ├── operating-rules.md                ← 常驻:双工具共享操作规则(hook 直注,ADR-0016)
```

- [ ] **Step 5: 写 ADR-0016(全文)**

`adr/0016-M4全局指令整合-共享规则单一源与native-stub派生.md`:

```markdown
# ADR-0016 · M4 全局指令整合:共享规则单一源 + native stub 软链派生

- 状态:Accepted
- 日期:2026-07-03
- 承继:ADR-0013(登记 M4 待决项)、ADR-0014(always-on 瘦身,本 ADR 定义豁免边界)
- 关联 spec:`docs/superpowers/specs/2026-07-03-M4全局指令整合-design.md`(D1–D8)

## 背景

`~/.claude/CLAUDE.md` 与 `~/.codex/AGENTS.md` 平行维护全量全局指令:共享内容双侧重复、改一处易漏另一处;文件在仓外不可版本化、双机不可同步——ADR-0013 登记的最后一个 P9 待决项。

## 决策

1. **共享规则单一源**:工具无关规则(个人偏好/Context7/指令文件维护/ConfigFile 审慎/dbq 只读通道/superpowers ask-first/跨工具 skill SOT/AiPalace 治理)收敛至 `vault/memory/00-RULES/operating-rules.md`;dbq 随规则进共享层,Codex 同获(用户拍板)。
2. **注入**:既有机制 A 扩展——`inject_index` 默认 files=`("00-RULES/operating-rules.md", "INDEX.md")`(法律在前、导航在后),双工具入口同源生效。
3. **native stub 入仓派生**:`context/native/{claude-global,codex-global}.md` 为真源,`~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md` 改软链(同 commands 模式);stub 只留工具专属规则(superpowers 注入前言/ConfigFile 第一道防线实现/Codex Workflow Memory Boundary)+ 兜底指针(未见 `[操作规则]` 注入块则手读 vault 绝对路径)。
4. **always-on 豁免边界**:operating-rules.md 是 ADR-0014 后 00-RULES 第二个获准 always-on 项。豁免理由:内容原本即经 native 文件 always-on,本次只换源+去双侧重复,净 token ≈ 持平或下降;此后再增 always-on 项仍须新 ADR。
5. 原件快照存档 `docs/archive/native-globals-2026-07-03/`(P8);本地另有 `.bak-m4` 副本,回滚 = 删软链恢复原件 + 还原 inject 默认元组。

## 后果

- 全局指令改一处即双工具生效;全局配置入 git,双机同步(Windows 侧软链仍属 evolution §6.1 既有待决)。
- hook 成为共享规则唯一注入通道(stub 兜底指针缓解单点);native 文件不再承载共享内容。

## 验证(P5 实测门)

- 软链后 `cat ~/.claude/CLAUDE.md`、`cat ~/.codex/AGENTS.md` 读出 stub;新会话见 `[操作规则]` + INDEX 双块有序无重复。
- hooks 16 例 + pytest 33 例 + memory 26 例全绿;doctor 无漂移。
- Codex 侧交互实测与 M5 收尾验证合并执行,结论回填本 ADR 验证节。
```

- [ ] **Step 6: 提交**

```bash
git add docs/governance/content-assets/vault.md docs/governance/evolution.md docs/governance/product-assets/injection.md vault/memory/INDEX.md "adr/0016-M4全局指令整合-共享规则单一源与native-stub派生.md"
git commit -m "docs(governance): M4 文档收口(vault/evolution/injection/INDEX)+ADR-0016"
```

---

### Task 5: 软链切换与机器实测门(⚠️ 主会话执行,合并 main 之后)

**Files:**
- 仓外副作用: `~/.claude/CLAUDE.md`、`~/.codex/AGENTS.md`(备份后替换为软链)
- 本地备份: `~/.claude/CLAUDE.md.bak-m4`、`~/.codex/AGENTS.md.bak-m4`(不入仓)

**Interfaces:**
- Consumes: Task 1–4 已合并 main(软链目标在 main 存在);终审与 finishing 流程已完成。

> 前置铁律:本任务**不派 subagent**;必须确认 `git -C ~/Library/CodeRepo/AI/AiPalace branch --show-current` 为 `main` 且 `context/native/` 双 stub 存在后才执行。

- [ ] **Step 1: 本地备份(回滚锚点)**

```bash
cp ~/.claude/CLAUDE.md ~/.claude/CLAUDE.md.bak-m4
cp ~/.codex/AGENTS.md ~/.codex/AGENTS.md.bak-m4
ls -la ~/.claude/CLAUDE.md.bak-m4 ~/.codex/AGENTS.md.bak-m4
```

Expected: 两备份存在且大小与原件一致。

- [ ] **Step 2: 切换软链**

```bash
rm ~/.claude/CLAUDE.md && ln -s ~/Library/CodeRepo/AI/AiPalace/context/native/claude-global.md ~/.claude/CLAUDE.md
rm ~/.codex/AGENTS.md && ln -s ~/Library/CodeRepo/AI/AiPalace/context/native/codex-global.md ~/.codex/AGENTS.md
ls -la ~/.claude/CLAUDE.md ~/.codex/AGENTS.md
```

Expected: 两软链各指向 context/native/ 真源。

- [ ] **Step 3: 机器实测门(D8 可机器验证部分)**

```bash
head -3 ~/.claude/CLAUDE.md && head -3 ~/.codex/AGENTS.md                    # 经软链读出 stub 受管头注
python3 ~/Library/CodeRepo/AI/AiPalace/tools/hooks/inject_index.py | python3 -c "import json,sys; t=json.load(sys.stdin)['hookSpecificOutput']['additionalContext']; assert '[操作规则]' in t and t.index('[操作规则]') < t.index('INDEX'); print('注入门 OK')"
wc -l ~/.claude/CLAUDE.md ~/.codex/AGENTS.md                                 # stub 应短(各 ≤20 行);共享正文若残留会显著超标
python3 ~/Library/CodeRepo/AI/AiPalace/tools/skillctl.py doctor 2>&1 | tail -1
```

Expected: stub 头注可见 / `注入门 OK` / 两 stub 各 ≤20 行 / doctor 全绿。另人工读一遍两 stub:允许出现"共享规则"指针语,不允许出现任何整段规则正文。

- [ ] **Step 4: 回滚预案(仅失败时)**

```bash
rm ~/.claude/CLAUDE.md && cp ~/.claude/CLAUDE.md.bak-m4 ~/.claude/CLAUDE.md
rm ~/.codex/AGENTS.md && cp ~/.codex/AGENTS.md.bak-m4 ~/.codex/AGENTS.md
# 并在仓内还原 inject_index.py 默认元组为 ("INDEX.md",) + revert 提交
```

- [ ] **Step 5: 账本登记**

`.superpowers/sdd/progress.md` 追加一行:`M4 Task 5: 软链切换完成(备份 .bak-m4 在位,机器实测门通过)`。

---

### Task 6: 用户收尾验证(⛔ gated,与 M5 试运行合并为一轮)

> 非 subagent 任务;由用户在日常使用中完成,主会话汇总回填。

- [ ] 新开 **Claude Code** 会话:确认系统上下文含 stub 受管头注 + `[操作规则]` 注入块 + INDEX 块(顺序正确、无重复)。
- [ ] **Codex** 会话:确认 AGENTS.md stub 生效(受管头注可见)+ `[操作规则]` 注入 + `/ai-palace` 可唤起 + `sweep` 可用(一轮验完 M4+M5 双待办)。
- [ ] 结论回填:ADR-0016 验证节补 Codex 实测结论;若软链不被识别 → 执行 Task 5 Step 4 回滚该侧 + 改硬拷贝派生 + evolution 登记待决项。
- [ ] 全部跑顺后:用户拍板执行 **M5-C(退役 /wrap,plan `2026-07-02-M5手动飞轮.md` Task 9)**。

---

## Self-Review 结论(已内联修正)

- **Spec 覆盖**:D1(Task 2+5 A+C 架构)/ D2(Task 1 dbq 入共享)/ D3(Task 1 共享收录 Cross-Tool+Governance,Task 2 codex stub 留 Memory Boundary)/ D4(Task 3 顺序+测试断言)/ D5(Task 4 ADR-0016 豁免边界 + vault.md/INDEX 常驻两项)/ D6(Task 2 stub 兜底指针)/ D7(Task 1 入仓快照 + Task 5 本地 .bak-m4 双备份)/ D8(Task 5 机器门 + Task 6 交互门与回退路径);spec §7 文档五处 = Task 4 五个 Step。
- **占位符**:无 TBD/TODO;所有新文件给全文,文档改动给精确替换文本。
- **一致性**:`[操作规则]` 锚点在 Task 1 H1 / Task 2 兜底指针 / Task 3 测试 / Task 5 注入门四处同字面;软链目标路径 Task 2 产出与 Task 5 命令一致;测试计数 hooks 15→16、pytest 32→33、memory 26 不变,Task 3/5 命令与之匹配。
